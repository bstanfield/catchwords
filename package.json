{
    "scripts": {
        "web": "cd frontend && npm i && npm start",
        "server": "cd backend && npm i && nodemon app.js"
    },
    "dependencies": {
        "concurrently": "^7.2.2",
        "nodemon": "^2.0.19"
    }
}