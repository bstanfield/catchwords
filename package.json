{
    "scripts": {
        "start": "cd backend && nodemon app.js",
        "heroku-postbuild": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "concurrently": "^7.2.2",
        "nodemon": "^2.0.19"
    }
}