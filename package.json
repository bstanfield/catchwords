{
    "scripts": {
        "start": "cd backend && npm install --force && node app.js",
        "heroku-postbuild": "echo this should run..."
    },
    "dependencies": {
        "concurrently": "^7.2.2",
        "nodemon": "^2.0.19"
    }
}