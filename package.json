{
    "scripts": {
        "start": "concurrently \"cd frontend && npm i && npm run build\" \"cd backend && npm i && npm start\""
    },
    "dependencies": {
        "concurrently": "^7.2.2",
        "nodemon": "^2.0.19"
    }
}